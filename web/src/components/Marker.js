const Marker = ({ position }) => {

    return (
        <div className="marker"><span class="material-symbols-outlined">
            logout
        </span></div>
    )
}

export default Marker;